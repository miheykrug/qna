<% if @answer.errors.present? %>
  $('.new-answer-errors').html('<%= j render 'shared/errors', resource: @answer %>')
<% else %>
  $('.new-answer-errors').html('')
  $('.answers').append('<%= j render @answer %>')
  $('.answer-form #answer_body').val('')
<% end %>